# Chapter 7 checkpoint

The baseline proves that a signature-only policy admits an untrusted, non-isolated builder.

The completed checkpoint verifies a complete source-to-target chain: trusted builder, provenance bound to Chapter 6 inputs, required evidence, and environment-specific admission.

It cannot prove that a real builder is isolated or that a registry preserves trustworthy provenance.
