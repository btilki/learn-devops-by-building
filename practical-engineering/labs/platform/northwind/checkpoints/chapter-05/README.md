# Chapter 5 checkpoint

The baseline proves that Fulfillment’s unofficial fork to skip a slow template drops inherited digest promotion and workload identity, and that a paved road with no supported exit is a cage.

The completed checkpoint verifies path steps, inherited artifact and identity defaults, scaffold coverage, computed conformance, and a supported exit that keeps remaining guardrails.

`make chapter-05-failure` injects an unofficial `latest-tag` fork onto the completed `fulfillment-api` entry and proves conformance still fails.

It cannot prove that a live repository was scaffolded.
