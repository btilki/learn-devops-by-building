{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "user-journey.schema.json",
  "type": "object",
  "required": ["schema_version", "kind", "id", "journeys"],
  "properties": {
    "schema_version": {"const": 1},
    "kind": {"const": "user-journeys"},
    "id": {"type": "string", "minLength": 1},
    "journeys": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "user", "failed_outcome", "owner", "later_proof"],
        "properties": {
          "id": {"type": "string", "pattern": "^[a-z0-9-]+$"},
          "user": {"type": "string", "minLength": 1},
          "failed_outcome": {"type": "string", "minLength": 1},
          "owner": {"type": "string", "minLength": 1},
          "later_proof": {"type": "string", "minLength": 1}
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
