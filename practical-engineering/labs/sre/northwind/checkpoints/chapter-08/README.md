# Chapter 8 checkpoint

The baseline proves that stamping `no_user_impact` on payment, paging `notification-service` at `storefront-oncall` as critical, and attributing warehouse to Storefront treats provider failure as theater and exhausts the wrong budget and rotation.

The completed checkpoint verifies payment journey-burn on Storefront, warehouse journey-burn on Fulfillment, non-critical recorded email, named timeouts and retry budgets, and a forbidden harmlessness claim.

It cannot query a real provider status page, and it does not prove that 2s / 5s / 10s or one retry are the right commercial bounds.
