# Chapter 7 checkpoint

The baseline proves that leaving toil unclassified, storing the bound as `we-are-busy`, emitting a capacity constant, and allowing `notification-service` as a critical SLO because on-call already watches email treats overload as permission.

The completed checkpoint verifies classified inventory, a numeric bound, a computed toil fraction that breaches 0.5, and a denied notification critical-SLO proposal against the Chapter 3 non-critical record.

It cannot measure real engineer hours and does not prove that 0.5 is the right commercial bound.
