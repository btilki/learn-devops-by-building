# Chapter 14 checkpoint

The baseline proves that applying newest mixed-region state, landing Fulfillment intent in Storefront, claiming DevOps reconstruction as recovery, missing numeric RTO and RPO, collapsing isolation, and emitting `status: recovered` leaves the portfolio unrecovered.

The completed checkpoint verifies fail-over from `region-primary` to `region-standby` against Chapter 12, explicit continue/freeze per tenant, rejected mixed replay, insufficient inherited restores and game-day tabletop, elapsed and RPO loss inside objectives, both protected journeys meeting catalog SLOs from independent fail-over observations, consumed Platform limitations, and computed **Evidence of portfolio recovery** without a self-emitted recovered or `slo_met` stamp.

It cannot prove a real multi-region fail-over.
