# Chapter 5 checkpoint

The baseline proves that paging `cpu-utilization` at `storefront-oncall` with forged `user_impact`, leaving `order_success_ratio` as a dashboard panel, and paging job-time at Storefront inverts journey burn and symptom noise.

The completed checkpoint verifies fast and slow journey burns page the matching catalog contacts, inherited DevOps window pairs are consumed, CPU and replica Ready do not page, and `time-to-first-environment` tickets `platform-oncall`.

It does not send a real page and cannot prove that 14.4 or 50 events are the right commercial thresholds.
