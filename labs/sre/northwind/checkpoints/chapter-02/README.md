# Chapter 2 checkpoint

The baseline proves that accepting `time-to-first-environment` as a portfolio SLO because leadership can see it leaves `order_success_ratio` as a tenant-workload non-metric and never accepts a Fulfillment dispatch indicator.

The completed checkpoint verifies accept, adjacent, and reject records against Chapter 1 journeys and refusals. It requires Storefront `order_success_ratio` and Fulfillment `dispatch_success_ratio` to be accepted, platform job-time proofs to stay adjacent with the recorded remaining owner, and component uptime to be rejected.

It cannot prove that the chosen indicators are objectively correct for a real company.
