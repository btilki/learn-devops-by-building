# Chapter 4 checkpoint

The baseline proves that a shared automation identity can inherit production authority without attributable human or workload subjects.

The completed checkpoint verifies stable human and automation subjects, issuer and audience binding, bounded roles, and reconstructable allow/deny traces.

Containment writes `build/chapter-04-contained.yaml` and does not revoke `compromised-session` in the operational register.

It cannot prove that a real identity provider evaluates the same claims or that revocation propagates at production speed.
