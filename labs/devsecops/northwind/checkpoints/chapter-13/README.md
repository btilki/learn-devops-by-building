# Chapter 13 checkpoint

The baseline proves that four valid signals with no detection hypothesis produce no alert.

The completed checkpoint verifies normalized evidence, correlation across independent producers, an owner, and a response action.

The evaluator joins modeled identity, supply-chain, and privilege events with the undeclared-egress event from `make chapter-12-attack`.

It cannot prove that a real telemetry backend retains complete evidence or that unmodeled attacker actions would be visible.
