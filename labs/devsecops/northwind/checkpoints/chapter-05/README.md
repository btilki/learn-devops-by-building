# Chapter 5 checkpoint

The baseline rejects a self-approved, unbounded emergency request whose lifecycle fields cannot have occurred in order.

The completed checkpoint verifies independent approval, bounded scope and duration, compensating controls, expiry, and review obligations for privileged operations.

It cannot prove that a real break-glass workflow prevents self-approval or that emergency access is removed after review.
