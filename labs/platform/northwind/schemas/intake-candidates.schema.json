{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "intake-candidates.schema.json",
  "type": "object",
  "required": ["schema_version", "kind", "id", "candidates"],
  "properties": {
    "schema_version": {"const": 1},
    "kind": {"const": "intake-candidates"},
    "id": {"type": "string", "minLength": 1},
    "candidates": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "summary"],
        "properties": {
          "id": {"type": "string", "pattern": "^[a-z0-9-]+$"},
          "summary": {"type": "string", "minLength": 1}
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
